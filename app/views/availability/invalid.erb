Sorry, your message "<%= request.subject %>"
couldn't be understood by the scheduler. 

<% participant.errors.each do |err| %>
- <%= err %>
<% end %>

<% if participant.availables.empty? %>
You must specify your availability in the body of the email.
<% end %>
See below for syntax rules.

-----
<%= template('availability/_usage', :event => event, :event_id => event_id, :list_id => list_id) %>