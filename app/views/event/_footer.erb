This email was generated by newman-scheduler

To share/update your availability for this event, send an email to:
  <%= availability_address(list_id, event_id)%>

To see the current best times for scheduling, send an email to:
  <%= schedule_address(list_id, event_id)%>

To propose a new event, send an email to:
  <%= new_event_address(list_id)%>
  
For general instructions, send an email to:
  <%= usage_address(list_id)%>